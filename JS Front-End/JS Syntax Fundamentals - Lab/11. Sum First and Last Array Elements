function sumFirstAndLast(numbers) {
  if (numbers.length < 2) {
    console.log("Error: the array must contain at least 2 numbers.");
    return;
  }
  const first = numbers[0];
  const last = numbers[numbers.length - 1];
  console.log(first + last);
}
